import React from "react";
import { Grid } from "semantic-ui-react";

const Home = () => {
  return (
    <Grid.Row>
      <Grid.Column width={16}>
        <h4>Home Page</h4>
      </Grid.Column>
    </Grid.Row>
  );
};

export default Home;
